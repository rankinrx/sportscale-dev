extends ../backend-layout
append styles
    // Custom styles for this template
    link(href='/css/dashboard.css', rel='stylesheet')
append body
     .row
        .col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
            h1.page-header #{athlete.name+ `'s History`}
            +panel-default
                .row
                    .col-md-12
                        .col-md-3
                            .input-group
                                input.form-control(type='text')
                                span.input-group-btn
                                    button.btn.btn-default(type='button') Search
                        .col-md-9
                            .btn-toolbar
                                .btn-group
                                button.btn.dropdown-toggle(data-toggle='dropdown', href='#' )
                                    | Columns
                                    span.caret
                                .btn-group
                                button.btn.dropdown-toggle(data-toggle='dropdown', href='#' )
                                    | Filter
                                    span.caret
                .row.top-buffer
                    .col-md-12
                        table
                            thead
                                tr: th Posts
                            tbody
                                each weight, i in athlete_weights
                                    tr
                                        td 
                                            a(href=' ') #{weight.weight}
                        //- ul.list-group
                        //-     each weight in athlete_weights
                        //-         li.list-group-item
                        //-             a(href='athlete.url') #{weight.date}
                        //-     else
                        //-         li.list-group-item There are no athletes.